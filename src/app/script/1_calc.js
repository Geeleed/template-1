const onIncrement = (num) => Number(num) + 1;

const onDecrement = (num) => Number(num) - 1;

const onIncrementByAmount = (num, addition) => Number(num) + Number(addition);

export { onIncrement, onDecrement, onIncrementByAmount };
